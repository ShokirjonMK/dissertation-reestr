# Redis

## Role
Cache/session/message layer (reserved for scale-oriented usage).

## Config
- `databases/redis/redis.conf`
- AOF enabled
- LRU eviction policy

## Current Usage
Redis is provisioned in infrastructure and ready for caching/session extensions.
